
/** 初始化 **/
function init() {
	initData(function() {
		initComponent();
		initListener();
		initFace();
	});
}

/** 初始化页面、内存等基本数据 **/
function initData(cb) {
	var a = PRQ.get("a");
	
	var ddd = 1;
	
	
	
	
	if(CU.isFunction(cb)) cb();
}

/** 初始化组件 **/
function initComponent() {
}

/** 对组件设置监听 **/
function initListener() {
}

/** 初始化界面 **/
function initFace() {
}






